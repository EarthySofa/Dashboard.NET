@model IEnumerable<Dashboard.Models.DummyTag>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.C_TagName_)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.C_Average_)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.C_Time_)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.C_TagName_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.C_Average_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.C_Time_)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.newcolumn }) |
            @Html.ActionLink("Details", "Details", new { id=item.newcolumn }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.newcolumn })
        </td>
    </tr>
}

</table>
